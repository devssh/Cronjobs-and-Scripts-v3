Some crontabs and scheduling info, random odd scripts.

Configuration needs to be extracted so that it works regardless of system.

Preliminary DevOps, nothing too fancy like Docker, Ansible, K8, VMWare, HDFS.
